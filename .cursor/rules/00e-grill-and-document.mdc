---
description: "Read when the user wants to stress-test a plan, challenge assumptions, lock decisions, build or update the project glossary, or record an architectural decision. Governs the default interview style for the entire 00a→00c planning flow."
globs: "**"
alwaysApply: true
---
<!-- Generated from .github/instructions/00e-grill-and-document.instructions.md — do not edit directly -->
# Grill & Document

Adapted with thanks from Matt Pocock's "grill-with-docs" skill (https://github.com/mattpocock/skills, MIT © 2026 Matt Pocock). PACAF-specific additions: Dataverse-aware glossary bridge, Code-App ADR trigger list, planning-payload integration.

## Grilling Cadence (default for all planning phases)
1. One **atomic** question at a time; never compound (no joining clauses with "and", "also", "plus", or a comma — split it)
2. Always supply the agent's recommended answer
3. When the question has multiple plausible answers, present them as a lettered list: `**A)** …`, `**B)** …`, `**C)** …` (one per line), mark your recommendation with `*(recommended)*`, and invite the user to reply with a letter — or multiple like `A, C` if more than one applies
4. Walk the design tree depth-first — resolve dependencies before moving sideways
5. If a question can be answered by reading code, solution metadata, or Dataverse schema, read instead of asking
6. Challenge against `CONTEXT.md` when the user's terminology conflicts with established glossary entries
7. Sharpen fuzzy language: propose a precise canonical term, update `CONTEXT.md` inline
8. Discuss concrete scenarios to probe edge cases
9. Cross-reference with code when the user states how something works

## CONTEXT.md — Living Glossary
- Lives at repo root. Created lazily on the first sharpened term.
- Pure business glossary — no implementation details, no specs.
- When a term is canonicalized, propose the PACAF bridge: glossary term → Dataverse `DisplayName` in `planning-payload.json` → `DataverseFieldLabel` `fallback` prop.
- Consult `CONTEXT.md` before introducing any new business term. Update inline, not in batches.
- Do not introduce `CONTEXT-MAP.md`. Single-context only.

## docs/adr/ — Architecture Decision Records
Offer an ADR only when all three hold: hard to reverse + surprising without context + real trade-off.

PACAF-specific qualifying decisions: publisher prefix, solution boundary, Dataverse vs SharePoint vs Custom Connector, environment topology, router choice deviation, Copilot Studio agent placement, Custom API vs Power Automate flow boundary, security model, seeding strategy.

Format: short title + 1–3 sentence paragraph. Optional: Status, Considered Options, Consequences. Sequential numbering in `docs/adr/`.

Full details: `.github/instructions/00e-grill-and-document.instructions.md`
