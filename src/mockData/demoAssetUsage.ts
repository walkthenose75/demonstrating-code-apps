// Rich prototype data lives in demoDataset.ts (generated usage graph).
// This file preserves the export name the mock provider imports.
import { demoAssetUsages } from '@/mockData/demoDataset';
import type { DemoAssetUsage } from '@/types/domain-models';

export const mockDemoAssetUsages: DemoAssetUsage[] = demoAssetUsages;
